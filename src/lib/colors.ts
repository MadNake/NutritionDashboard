/** Macro/metric colors, mapped to the theme chart tokens (see index.css). */
export const COLORS = {
  calories: "var(--chart-1)", // amber (= primary)
  protein: "var(--chart-2)", // green
  carbs: "var(--chart-3)", // blue
  fat: "var(--chart-4)", // violet
  fiber: "var(--chart-5)", // teal
} as const;
